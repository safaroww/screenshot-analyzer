{
  "version": 2,
  "builds": [
    {
      "src": "api/**/*.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/health", "dest": "/api/health" },
    { "src": "/analyze", "dest": "/api/analyze" },
    { "src": "/api/(.*)", "dest": "/api/$1" }
  ]
}
